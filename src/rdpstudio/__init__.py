"""RDP Studio — a cross-platform remote-access workbench.

Tabbed SSH/SFTP sessions for Linux hosts and RDP sessions for Windows hosts,
with a credential vault, port forwarding, file transfer and a plugin-ready
protocol architecture.
"""

__version__ = "0.9.0"

APP_NAME = "RDP Studio"
ORG_NAME = "RDPStudio"
