"""KB-Remote — a cross-platform remote-access workbench.

Tabbed SSH/SFTP/OpenSSH sessions for Linux, Windows, BSD and macOS hosts, plus RDP sessions for Windows hosts,
with file transfer and a plugin-ready protocol architecture.
"""

__version__ = "0.9.0"

APP_NAME = "KB-Remote"
ORG_NAME = "KB-Remote"
